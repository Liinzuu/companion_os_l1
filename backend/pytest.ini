[pytest]
# Run pytest from this backend/ folder with:  python -m pytest -v
# - testpaths: where pytest looks for tests (under apps/, next to the code).
# - python_files / python_functions: pytest treats files named test_*.py
#   and functions named test_* as tests.
testpaths = apps
python_files = test_*.py
python_functions = test_*
